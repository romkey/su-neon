<%= form_with(model: news_source, local: true) do |form| %>
  <% if news_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_source.errors.count, "error") %> prohibited this news_source from being saved:</h2>

      <ul>
      <% news_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :news_source_name %>
  </div>

  <div class="field">
    <%= form.label :feed_url %>
    <%= form.text_field :feed_url, id: :news_source_feed_url %>
  </div>

  <div class="field">
    <%= form.label :last_processed_at %>
    <%= form.datetime_select :last_processed_at, id: :news_source_last_processed_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
